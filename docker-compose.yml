version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "9000:3000"
    env_file:
      - .env
    volumes:
      - ./fabric-samples:/app/fabric-samples
    environment:
      - FABRIC_TLS_CERT_PATH=/app/fabric-samples/test-network/organizations/peerOrganizations/org1.example.com/peers/peer0.org1.example.com/tls/ca.crt
    depends_on:
      - mongodb
      # - redis
      - sftp
    networks:
      - cnr-network

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
      - ./mongo-init:/docker-entrypoint-initdb.d
    env_file:
      - .env
    networks:
      - cnr-network

  # redis:
  #   image: redis:alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:`
  #     - redis_data:/data
  #   networks:
  #     - cnr-network

  sftp:
    image: atmoz/sftp
    ports:
      - "2223:22"
    volumes:
      - ./sftp/data:/home/${SFTP_USER}/upload
    command: ${SFTP_USER}:${SFTP_PASS}:::${SFTP_USER}
    env_file:
      - .env
    networks:
      - cnr-network

  frontend:  
    build:
      context: ./frontend/cnr
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - cnr-network

volumes:
  mongodb_data:
  # redis_data:

networks:
  cnr-network:
    driver: bridge
